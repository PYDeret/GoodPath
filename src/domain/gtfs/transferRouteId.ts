/**
 * Sentinel `routeId` for interchange edges added by
 * scripts/gtfs/transform.mjs from GTFS transfers.txt (walking between
 * platforms/lines at the same station complex, not a ride on a route).
 */
export const TRANSFER_ROUTE_ID = 'TRANSFER';
